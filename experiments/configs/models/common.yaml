out_dir: results
wandb:
  use: True
  project: experiments
  entity: chienn
optim:
  clip_grad_norm: True
  optimizer: adamW
  weight_decay: 1e-5
  base_lr: 1e-4
  max_epoch: 100
  scheduler: cosine_with_warmup
  num_warmup_epochs: 10